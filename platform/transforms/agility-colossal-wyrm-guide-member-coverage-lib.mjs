const unique=values=>[...new Set(values)];
export function auditAgilityColossalWyrmGuideMemberCoverage({members=[],guideCandidates=[]}){
  const guideRevisions=unique(members.map(member=>member.guideSourceRevision).filter(Boolean)),sectionKeys=unique(members.map(member=>member.sectionKey).filter(Boolean)),sectionKey=sectionKeys[0]||null,sectionCandidates=guideCandidates.filter(candidate=>candidate.source_section_key===sectionKey),memberDetails=members.map(member=>{const candidates=sectionCandidates.filter(candidate=>candidate.candidate_key===member.candidateKey),identityBlockers=[];if(candidates.length!==1)identityBlockers.push(candidates.length?'duplicate_same_revision_guide_candidates':'same_revision_guide_candidate_missing');if(candidates.some(candidate=>candidate.source_revision!==member.guideSourceRevision))identityBlockers.push('guide_candidate_revision_mismatch');if(candidates.some(candidate=>candidate.minimum_agility!==member.minimumAgility))identityBlockers.push('guide_candidate_minimum_agility_mismatch');if(member.corroboratingMinimumAgility?.some(level=>level!==member.minimumAgility))identityBlockers.push('cross_source_minimum_agility_mismatch');return {memberKey:member.memberKey,candidateKey:member.candidateKey,name:member.name,sourceOrder:member.sourceOrder,routePolicy:member.routePolicy,minimumAgility:member.minimumAgility,guideCandidateKeys:candidates.map(candidate=>candidate.candidate_key),sourceRevisions:member.sourceRevisions,coursePageMarkedObsolete:member.coursePageMarkedObsolete,rateClaims:member.rateClaims,lapXpClaims:member.lapXpClaims,timingClaims:member.timingClaims,mechanicalBlockers:member.mechanicalBlockers||[],sourceLocators:member.sourceLocators,status:identityBlockers.length?'route_identity_blocked':'route_identity_covered',identityBlockers}}),expected=new Set(members.map(member=>member.candidateKey)),unexpectedSectionCandidateKeys=unique(sectionCandidates.filter(candidate=>!expected.has(candidate.candidate_key)).map(candidate=>candidate.candidate_key)),uncovered=memberDetails.filter(member=>member.identityBlockers.length),identityBlockers=[];
  if(guideRevisions.length!==1)identityBlockers.push('guide_member_revision_scope_invalid');if(sectionKeys.length!==1)identityBlockers.push('guide_member_section_scope_invalid');if(members.length!==2)identityBlockers.push('expected_two_colossal_wyrm_route_members');if(uncovered.length)identityBlockers.push('one_or_more_colossal_wyrm_routes_lack_exact_same_revision_candidate');if(unexpectedSectionCandidateKeys.length)identityBlockers.push('guide_section_candidate_not_present_in_route_inventory');
  const internalMemberAuditSatisfied=identityBlockers.length===0,mechanicalBlockers=unique(memberDetails.flatMap(member=>member.mechanicalBlockers)),mechanicalCompletenessProven=internalMemberAuditSatisfied&&mechanicalBlockers.length===0;
  return {contract:'sensum.agility-colossal-wyrm-guide-member-coverage-audit.v1',sectionKey,guideSourceRevision:guideRevisions.length===1?guideRevisions[0]:null,memberCount:members.length,coveredMemberCount:memberDetails.length-uncovered.length,uncoveredMemberCount:uncovered.length,routeIdentityCoverageOnly:true,internalMemberAuditSatisfied,mechanicalCompletenessProven,memberDetails,unexpectedSectionCandidateKeys,identityBlockers,mechanicalBlockers,blockers:[...identityBlockers,...mechanicalBlockers],absoluteBestGate:mechanicalCompletenessProven?'eligible_for_performance_review':'blocked_conflicting_or_obsolete_colossal_wyrm_mechanics'};
}
