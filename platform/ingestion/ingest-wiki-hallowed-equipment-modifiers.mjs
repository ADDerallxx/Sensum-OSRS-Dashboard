import path from 'node:path';import {audit,hash,writeSnapshot} from './lib.mjs';import {wikiRevisions,WIKI_API} from './activity-evidence-lib.mjs';import {parseHallowedEquipmentModifiers} from './hallowed-equipment-modifier-lib.mjs';
const root=path.resolve(process.argv.find(x=>x.startsWith('--out='))?.slice(6)||'.platform-data'),titles=['Hallowed Sepulchre'],pages=await wikiRevisions(titles),records=[];
for(const page of pages){const revision=page.revisions?.[0];if(!revision)continue;const sourceUrl=`https://oldschool.runescape.wiki/w/${encodeURIComponent(page.title.replace(/ /g,'_'))}`;for(const row of parseHallowedEquipmentModifiers({title:page.title,content:revision.slots?.main?.content||'',sourceRevision:revision.revid,sourceTimestamp:revision.timestamp,sourceUrl}))records.push({...row,content_hash:hash(row)})}
const report=audit(records,{minimum:13,required:['parent_name','variant_key','record_kind','axis_coverage','requirements','source_revision','source_locator'],maximumUnknownRatio:0}),snapshot=await writeSnapshot(root,'hallowed-equipment-variants',records,{kind:'osrs_wiki_hallowed_equipment_modifiers',api:WIKI_API,pages:titles,audit:report});console.log(JSON.stringify({manifest:snapshot.manifest,publishable:report.publishable,records:records.length,findings:report.findings},null,2));
