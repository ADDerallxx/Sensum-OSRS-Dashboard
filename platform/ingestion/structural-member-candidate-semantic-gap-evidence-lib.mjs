import { hash } from './lib.mjs';
import { parseRootTemplateSignatures } from './unlock-linked-page-source-signature-lib.mjs';

const REQUIRED_RULES=[
  'oneEvidenceRecordPerEligibleRoutingRecord','oneEvidencePacketPerRoutedWorkItem','everyDistinctPinnedSourceRevisionMustBeRefetchedFromTheOfficialWikiApi','exactRevisionIdentityHashBytesAndTimestampMustRevalidate','sourcePoolMustDeduplicateOnlyExactPageAndRevisionIdentity','everyRequiredChannelMustReceiveAnExplicitObservationState','missingEvidenceAndPendingReviewMustRemainDistinct','packetCaptureCompletenessDoesNotMeanSemanticEvidenceCompleteness','candidateIdentityPageTypeRelationshipMembershipMappingAndUniverseRemainSeparate','globalWeightLanguageCannotBeJoinedToCandidateOccurrencesAsMembershipProof','arithmeticCountEqualityCannotProveMappingOrCompleteness','nonExhaustiveSourceLanguageBlocksCompleteUniverseClaims','sourceSilenceCannotBecomeANegativeGameFact','reviewDecisionsAndAllSemanticVerdictsRemainNull','inputEvidenceDispositionsRoutingAndPriorSourcesMustBePreserved','namesTitlesPageIdsCandidateKeysLabelsAliasesAndOverridesCannotAlterPolicyBehavior','memberRepeatabilityMechanicsAndOptimizerPromotionAreForbidden','currentAccountStateIsForbidden'
];
const BRANCHES=[
  'reviewed_structural_candidate_member_identity_and_variant_scope_evidence',
  'unresolved_source_page_entity_type_evidence',
  'structural_parent_relationship_reconciliation_evidence',
  'weighted_parent_task_entry_membership_evidence',
  'declared_total_one_to_one_member_mapping_evidence',
  'complete_member_universe_reconciliation_evidence'
];
const STAGE_FIELDS=new Set(['contract','contentHash','blockers','state','sourceStructuralMemberCandidateSemanticGapEvidenceWorkRoutingContentHash','structuralMemberCandidateSemanticGapEvidenceSources','structuralMemberCandidateSemanticGapEvidencePackets','structuralMemberCandidateSemanticGapEvidence','structuralMemberCandidateSemanticGapEvidenceReview']);
const unique=values=>[...new Set(values)];
const sorted=values=>[...values].sort((a,b)=>String(a).localeCompare(String(b)));
const duplicates=values=>unique(values.filter((value,index)=>values.indexOf(value)!==index));
const preservedInput=record=>Object.fromEntries(Object.entries(record||{}).filter(([key])=>!STAGE_FIELDS.has(key)));
const sourceKey=identity=>`wiki-pageid:${identity.sourcePageId}|revision:${identity.sourceRevision}`;

function forbiddenPolicyPaths(policy={}){const findings=[],forbidden=/^(?:pageId|pageIds|revision|revisions|activityName|activityNames|canonicalLabel|canonicalLabels|candidateKey|candidateKeys|memberCandidateKey|memberCandidateKeys|title|titles|label|labels|alias|aliases|override|overrides)$/i;const walk=(value,at='')=>{if(Array.isArray(value))return value.forEach((child,index)=>walk(child,`${at}[${index}]`));if(!value||typeof value!=='object')return;for(const [key,child]of Object.entries(value)){const next=at?`${at}.${key}`:key;if(forbidden.test(key))findings.push(next);walk(child,next)}};walk(policy);return sorted(unique(findings));}

export function compileStructuralMemberCandidateSemanticGapEvidencePolicy(policy={}){
  const invalidRules=REQUIRED_RULES.filter(rule=>policy.rules?.[rule]!==true);if(policy.rules?.automaticVerificationAllowed!==false)invalidRules.push('automaticVerificationAllowed');
  const contractValid=policy.inputContract==='sensum.structural-member-candidate-semantic-gap-evidence-work-routing.v1'&&policy.recordContract==='sensum.structural-member-candidate-semantic-gap-evidence.v1'&&policy.auditContract==='sensum.structural-member-candidate-semantic-gap-evidence-audit.v1'&&policy.inputState==='structural_member_candidate_semantic_gap_evidence_work_routed_gates_closed';
  const capture=policy.sourceCapture||{},invalidSourceCapture=[];if(capture.apiNamespace!==0)invalidSourceCapture.push('apiNamespace');if(!Number.isInteger(capture.revisionBatchSize)||capture.revisionBatchSize<1||capture.revisionBatchSize>50)invalidSourceCapture.push('revisionBatchSize');for(const field of ['retainCompleteExactRevisionSourceText','requirePageIdRevisionTimestampTitleUrlHashAndBytes','deduplicateExactPageRevisionSources'])if(capture[field]!==true)invalidSourceCapture.push(field);
  const forbidden=forbiddenPolicyPaths(policy);return {valid:contractValid&&!invalidRules.length&&!invalidSourceCapture.length&&!forbidden.length,contractValid,invalidRules:unique(invalidRules),invalidSourceCapture:unique(invalidSourceCapture),forbiddenPolicyPaths:forbidden};
}

function allWorkItems(route={}){return [...(route.candidateIdentityAndVariantScopeWorkItems||[]),...(route.sourcePageEntityTypeWorkItems||[]),...(route.structuralParentRelationshipReconciliationWorkItems||[]),...(route.weightedParentTaskEntryMembershipWorkItems||[]),...(route.parentCompletenessWorkItems||[])];}
function inputEligible(record,policy){const route=record.structuralMemberCandidateSemanticGapEvidenceWorkRouting||{},items=allWorkItems(route);return record.contract===policy.inputContract&&record.state===policy.inputState&&record.accountIndependent===true&&route.routeState==='routed_blocked_structural_candidate_semantic_gap_evidence_work_not_completed'&&route.totalWorkItemCount===items.length&&route.blockedWorkItemCount===items.length&&route.completedWorkItemCount===0&&route.evidenceKeyCount===0&&route.evidenceWorkComplete===false&&route.memberUniverseComplete===false&&route.automaticVerificationApplied===false&&items.length>0&&items.every(item=>BRANCHES.includes(item.branchKey)&&Array.isArray(item.requiredChannels)&&item.requiredChannels.length===4&&Array.isArray(item.evidenceKeys)&&item.evidenceKeys.length===0&&item.evidenceWorkComplete===false&&item.automaticVerificationApplied===false)&&Array.isArray(record.structuralMemberCandidateIdentityEvidenceSources)&&record.structuralMemberCandidateIdentityEvidenceSources.length>0&&Array.isArray(record.parentMemberInventoryEvidenceSources)&&record.parentMemberInventoryEvidenceSources.length>0&&record.memberExpansionReview?.state==='unreviewed'&&record.mechanicsReview?.state==='unreviewed'&&record.optimizerEligible===false;}
export function selectStructuralMemberCandidateSemanticGapEvidenceInputs(records=[],policy={}){return records.filter(record=>inputEligible(record,policy));}

function sourceDescriptor(identity,text,role,context){return {sourceKey:sourceKey(identity),sourcePageId:Number(identity.sourcePageId),resolvedTitle:identity.resolvedTitle,sourceRevision:String(identity.sourceRevision),sourceTimestamp:identity.sourceTimestamp,sourceUrl:identity.sourceUrl,sourceContentHash:identity.sourceContentHash,sourceContentBytes:Number(identity.sourceContentBytes??Buffer.byteLength(String(text||''),'utf8')),expectedExactRevisionSourceText:String(text||''),roles:[role],contexts:[context]};}
function sourceDescriptorsFor(record){const rows=[];for(const packet of record.structuralMemberCandidateIdentityEvidenceSources||[])rows.push(sourceDescriptor(packet.sourcePageIdentity,packet.exactRevisionSourceText,'candidate_subject_source',{structuralCandidateKey:packet.structuralCandidateKey,workItemKey:packet.workItemKey}));for(const packet of record.parentMemberInventoryEvidenceSources||[])rows.push(sourceDescriptor(packet.sourcePageIdentity,packet.exactRevisionSourceText,'parent_inventory_source',{sourceEvidenceKey:packet.sourceEvidenceKey}));const groups=new Map();for(const row of rows){const existing=groups.get(row.sourceKey);if(!existing){groups.set(row.sourceKey,row);continue}existing.roles=sorted(unique([...existing.roles,...row.roles]));existing.contexts.push(...row.contexts);if(hash({pageId:existing.sourcePageId,title:existing.resolvedTitle,revision:existing.sourceRevision,timestamp:existing.sourceTimestamp,url:existing.sourceUrl,contentHash:existing.sourceContentHash,bytes:existing.sourceContentBytes,text:existing.expectedExactRevisionSourceText})!==hash({pageId:row.sourcePageId,title:row.resolvedTitle,revision:row.sourceRevision,timestamp:row.sourceTimestamp,url:row.sourceUrl,contentHash:row.sourceContentHash,bytes:row.sourceContentBytes,text:row.expectedExactRevisionSourceText}))existing.sourceConflict=true;}return sorted([...groups.values()].map(row=>({...row,contexts:row.contexts.sort((a,b)=>hash(a).localeCompare(hash(b)))}))).sort((a,b)=>Number(a.sourcePageId)-Number(b.sourcePageId)||Number(a.sourceRevision)-Number(b.sourceRevision));}
export function discoverStructuralMemberCandidateSemanticGapExactRevisionRequests(records=[],policy={}){const sources=selectStructuralMemberCandidateSemanticGapEvidenceInputs(records,policy).flatMap(sourceDescriptorsFor),groups=new Map();for(const source of sources){const existing=groups.get(source.sourceKey);if(!existing)groups.set(source.sourceKey,{sourceKey:source.sourceKey,sourcePageId:source.sourcePageId,sourceRevision:source.sourceRevision,contexts:[...source.contexts]});else existing.contexts.push(...source.contexts);}return [...groups.values()].sort((a,b)=>Number(a.sourceRevision)-Number(b.sourceRevision)).map(row=>({...row,contexts:row.contexts.sort((a,b)=>hash(a).localeCompare(hash(b)))}));}

function responseRevisionMap(batches=[]){const map=new Map();for(const batch of batches)for(const page of batch.response?.query?.pages||[])for(const revision of page.revisions||[])map.set(String(revision.revid),{page,revision});return map;}
function templateObservations(text=''){const source=String(text),rows=[];let depth=0;for(let index=0;index<source.length-1;index++){const pair=source.slice(index,index+2);if(pair==='{{'&&source[index+2]!=='{'){const start=index+2;let end=start;while(end<source.length&&!/[|}\r\n]/.test(source[end]))end++;const name=source.slice(start,end).replace(/^subst\s*:/i,'').trim().replace(/\s+/g,' ');if(name&&!name.startsWith('#')&&!name.includes('{'))rows.push({template:name,templateKey:name.toLowerCase(),nestingDepth:depth+1,line:source.slice(0,index).split(/\r?\n/).length});depth++;index++;continue}if(pair==='}}'&&depth>0){depth--;index++;}}return rows;}
function exactSourcePacket(expected,resolution,contentHash){const page=resolution?.page,revision=resolution?.revision,text=revision?.slots?.main?.content,checks={responseRevisionPresent:Boolean(page&&revision),mainNamespace:page?.ns===0,pageIdMatches:Number(page?.pageid)===expected.sourcePageId,revisionMatches:String(revision?.revid||'')===expected.sourceRevision,titleMatches:page?.title===expected.resolvedTitle,timestampMatches:revision?.timestamp===expected.sourceTimestamp,exactTextPresent:typeof text==='string',contentHashMatches:typeof text==='string'&&contentHash(text)===expected.sourceContentHash,contentBytesMatch:typeof text==='string'&&Buffer.byteLength(text,'utf8')===expected.sourceContentBytes,inputExactTextMatches:typeof text==='string'&&text===expected.expectedExactRevisionSourceText,noSourceConflict:expected.sourceConflict!==true},complete=Object.values(checks).every(Boolean);return {sourceKey:expected.sourceKey,roles:expected.roles,contexts:expected.contexts,sourcePageIdentity:{sourcePageId:expected.sourcePageId,resolvedTitle:expected.resolvedTitle,sourceRevision:expected.sourceRevision,sourceTimestamp:expected.sourceTimestamp,sourceUrl:expected.sourceUrl,sourceContentHash:expected.sourceContentHash,sourceContentBytes:expected.sourceContentBytes},exactRevisionSourceText:typeof text==='string'?text:null,rootTemplateSignatures:typeof text==='string'?parseRootTemplateSignatures(text):[],templateNameObservations:typeof text==='string'?templateObservations(text):[],integrityChecks:checks,captureComplete:complete,deficiencies:Object.entries(checks).filter(([,value])=>!value).map(([key])=>key),semanticVerdict:null,state:complete?'complete_exact_revision_source_capture_non_verdict':'blocked_exact_revision_source_capture_incomplete'};}

function candidateDisposition(record,item){return (record.structuralMemberCandidateIdentityDispositions||[]).find(candidate=>candidate.structuralCandidateKey===item.structuralCandidateKey)||null;}
function candidateSourceKey(item){return item.sourceBoundaryContext?.sourcePageIdentity?sourceKey(item.sourceBoundaryContext.sourcePageIdentity):null;}
function parentSourceKeys(record){return (record.parentMemberInventoryEvidenceSources||[]).map(packet=>sourceKey(packet.sourcePageIdentity));}
function observation(channel,item,record,sourcesByKey){const disposition=candidateDisposition(record,item),candidateKey=candidateSourceKey(item),parents=parentSourceKeys(record),candidateSource=candidateKey?sourcesByKey.get(candidateKey):null,parentSources=parents.map(key=>sourcesByKey.get(key)).filter(Boolean),candidateRefs=candidateKey?[candidateKey]:[],allRefs=unique([...candidateRefs,...parents]);let captureState='authoritative_evidence_not_observed_in_defined_source_set',evidence={};
  if(channel==='exact_subject_declaration'){captureState='revision_pinned_source_page_subject_observation_available_semantic_review_required';evidence={sourcePageIdentity:candidateSource?.sourcePageIdentity||item.sourceBoundaryContext?.sourcePageIdentity||null};}
  else if(channel==='variant_and_multi_type_scope'){captureState='revision_pinned_structural_observations_available_semantic_review_required';evidence={sourcePageEntityTypeState:disposition?.sourcePageEntityType?.state||item.sourceBoundaryContext?.sourcePageEntityTypeState||null,sourcePageEntityTypes:disposition?.sourcePageEntityType?.entityTypes||item.sourceBoundaryContext?.sourcePageEntityTypes||[],rootTemplateSignatures:candidateSource?.rootTemplateSignatures||[],templateNameObservations:candidateSource?.templateNameObservations||[]};}
  else if(channel==='source_role_to_subject_binding'||channel==='exact_parent_occurrence'||channel==='candidate_role_scope'||channel==='source_page_type_coherence'){captureState='revision_pinned_parent_and_candidate_structure_observations_available_review_required';evidence={candidateRole:item.candidateRole||null,structuralParentRelationship:disposition?.structuralParentRelationship||null,sourceBoundaryContext:item.sourceBoundaryContext||null};}
  else if(channel==='variant_container_structure'||channel==='nested_subject_variant_binding'||channel==='exact_top_level_subject_scope'){captureState='revision_pinned_template_structure_observations_available_but_subject_variant_binding_unresolved';evidence={rootTemplateSignatures:candidateSource?.rootTemplateSignatures||[],templateNameObservations:candidateSource?.templateNameObservations||[]};}
  else if(channel==='explicit_weighted_entry_membership'){captureState='revision_pinned_candidate_occurrence_and_global_weight_observations_insufficient_cross_scope_join_forbidden';evidence={candidateOccurrence:disposition?.structuralParentRelationship?.evidence?.sourceCandidateEvidence||null,declaredTotalAssessments:record.parentMemberInventoryDisposition?.declaredTotalAssessments||[],distributionAssessments:record.parentMemberInventoryDisposition?.distributionAssessments||[]};}
  else if(channel==='parent_selection_scope'||channel==='declared_total_scope_and_revision'){captureState='revision_pinned_parent_selection_scope_observations_available_review_required';evidence={parentSources:parentSources.map(source=>source.sourcePageIdentity),declaredTotalAssessments:record.parentMemberInventoryDisposition?.declaredTotalAssessments||[],distributionAssessments:record.parentMemberInventoryDisposition?.distributionAssessments||[]};}
  else if(channel==='source_limitation_reconciliation'||channel==='complete_parent_member_enumeration'){captureState='revision_pinned_source_limitation_observed_reconciliation_unresolved';evidence={nonExhaustiveQualificationAssessments:record.parentMemberInventoryDisposition?.nonExhaustiveQualificationAssessments||[],arithmeticAssessment:record.parentMemberInventoryDisposition?.arithmeticAssessment||null};}
  else if(channel==='duplicate_alias_and_variant_treatment'||channel==='explicit_parent_member_mapping'||channel==='independent_authoritative_coverage_check'){captureState='authoritative_evidence_not_observed_in_defined_source_set';evidence={sourceSilenceIsNotNegativeEvidence:true};}
  else if(channel.endsWith('_decision')||channel.endsWith('_review')){captureState='review_decision_pending';evidence={automaticReviewForbidden:true};}
  return {channel,captureState,sourceEvidenceKeys:allRefs,observationCaptured:true,sufficientForSemanticVerdict:false,semanticVerdict:null,evidence};}

function packetFor(item,record,sourcesByKey){const channels=item.requiredChannels.map(channel=>observation(channel,item,record,sourcesByKey)),sourceEvidenceKeys=unique(channels.flatMap(channel=>channel.sourceEvidenceKeys)),sourcesComplete=sourceEvidenceKeys.length>0&&sourceEvidenceKeys.every(key=>sourcesByKey.get(key)?.captureComplete===true),capturePacketComplete=sourcesComplete&&channels.length===item.requiredChannels.length&&channels.every(channel=>channel.observationCaptured===true);return {packetKey:`${item.workItemKey}|revision-pinned-semantic-gap-evidence`,workItemKey:item.workItemKey,branchKey:item.branchKey,workKind:item.workKind,structuralCandidateKey:item.structuralCandidateKey??null,candidateRole:item.candidateRole??null,requiredChannels:item.requiredChannels,sourceBoundaryContext:item.sourceBoundaryContext,sourceEvidenceKeys,channelObservations:channels,capturePacketComplete,semanticEvidenceComplete:false,candidateMemberIdentityVerdict:null,sourcePageEntityTypeVerdict:null,structuralParentRelationshipVerdict:null,weightedTaskEntryMembershipVerdict:null,mappingVerdict:null,inventoryCompletenessVerdict:null,memberUniverseComplete:false,evidenceWorkComplete:false,automaticVerificationApplied:false,state:capturePacketComplete?'revision_pinned_evidence_observations_captured_semantic_review_pending':'blocked_incomplete_revision_pinned_evidence_capture'};}

function expectedRecord(input,revisionBatches,policy,contentHash){const descriptors=sourceDescriptorsFor(input),responses=responseRevisionMap(revisionBatches),sources=descriptors.map(expected=>exactSourcePacket(expected,responses.get(expected.sourceRevision),contentHash)),sourcesByKey=new Map(sources.map(source=>[source.sourceKey,source])),items=allWorkItems(input.structuralMemberCandidateSemanticGapEvidenceWorkRouting),packets=items.map(item=>packetFor(item,input,sourcesByKey)),captureComplete=sources.every(source=>source.captureComplete)&&packets.every(packet=>packet.capturePacketComplete),channelObservations=packets.flatMap(packet=>packet.channelObservations),{contentHash:inputHash,blockers:inputBlockers=[],...rest}=input;return {contract:policy.recordContract,...preservedInput(rest),sourceStructuralMemberCandidateSemanticGapEvidenceWorkRoutingContentHash:inputHash,structuralMemberCandidateSemanticGapEvidenceSources:sources,structuralMemberCandidateSemanticGapEvidencePackets:packets,structuralMemberCandidateSemanticGapEvidence:{evidenceState:captureComplete?'complete_revision_pinned_semantic_gap_source_capture_semantic_reviews_pending':'incomplete_revision_pinned_semantic_gap_source_capture',distinctRevisionPinnedSourceCount:sources.length,completeRevisionPinnedSourceCount:sources.filter(source=>source.captureComplete).length,workItemEvidencePacketCount:packets.length,completeCapturePacketCount:packets.filter(packet=>packet.capturePacketComplete).length,channelObservationCount:channelObservations.length,missingAuthoritativeEvidenceObservationCount:channelObservations.filter(item=>item.captureState==='authoritative_evidence_not_observed_in_defined_source_set').length,pendingReviewObservationCount:channelObservations.filter(item=>item.captureState==='review_decision_pending').length,insufficientObservationCount:channelObservations.filter(item=>item.captureState.includes('insufficient')||item.captureState.includes('unresolved')).length,captureCoverageComplete:captureComplete,semanticEvidenceWorkComplete:false,candidateMemberIdentityVerdictCount:0,sourcePageEntityTypeVerdictCount:0,structuralParentRelationshipVerdictCount:0,weightedTaskEntryMembershipVerdictCount:0,mappingVerdictCount:0,inventoryCompletenessVerdictCount:0,memberUniverseComplete:false,automaticVerificationApplied:false},structuralMemberCandidateSemanticGapEvidenceReview:{state:'unreviewed_revision_pinned_semantic_gap_evidence_packets_ready_for_separate_branch_reviews',captureCoverageComplete:captureComplete,semanticEvidenceWorkComplete:false,candidateMemberIdentityVerdict:null,sourcePageEntityTypeVerdict:null,structuralParentRelationshipVerdict:null,weightedTaskEntryMembershipVerdict:null,mappingVerdict:null,inventoryCompletenessVerdict:null,memberUniverseComplete:false,automaticVerificationApplied:false},accountIndependent:true,blockers:unique([...inputBlockers,captureComplete?'revision_pinned_structural_member_candidate_semantic_gap_evidence_captured_reviews_pending':'one_or_more_revision_pinned_semantic_gap_evidence_packets_incomplete','candidate_member_identity_and_variant_scope_review_pending',...(items.some(item=>item.branchKey==='unresolved_source_page_entity_type_evidence')?['one_or_more_candidate_source_pages_remain_untyped']:[]),'weighted_parent_task_entry_membership_not_proven','source_inventory_non_exhaustive_qualification_prevents_completeness','one_to_one_mapping_between_structural_candidates_and_declared_total_not_proven','member_universe_completeness_not_proven','all_repeatability_evidence_domains_remain_unresolved','requirements_xp_timing_and_mechanics_not_structured','independent_complete_activity_universe_not_established']),state:captureComplete?'structural_member_candidate_semantic_gap_revision_pinned_evidence_captured_semantic_reviews_pending_gates_closed':'structural_member_candidate_semantic_gap_revision_pinned_evidence_capture_incomplete_gates_closed'};}

function accountStateFindings(records){const forbidden=/^(?:currentBaseLevel|currentLevel|currentXp|username|accountName|bank|bankItems|ownedEquipment|currentAccount)$/i,findings=[];const walk=(value,at='')=>{if(Array.isArray(value))return value.forEach((child,index)=>walk(child,`${at}[${index}]`));if(!value||typeof value!=='object')return;for(const [key,child]of Object.entries(value)){const next=at?`${at}.${key}`:key;if(forbidden.test(key))findings.push(next);walk(child,next)}};records.forEach((record,index)=>walk(record,`[${index}]`));return findings;}
function revisionRequestCoverage(inputs,batches,policy){const expected=discoverStructuralMemberCandidateSemanticGapExactRevisionRequests(inputs,policy).map(item=>item.sourceRevision),submitted=batches.flatMap(batch=>batch.requestedRevisions||[]).map(String),duplicateSubmittedRevisions=duplicates(submitted),missingSubmittedRevisions=expected.filter(item=>!submitted.includes(item)),unexpectedSubmittedRevisions=submitted.filter(item=>!expected.includes(item)),invalidBatches=batches.flatMap((batch,index)=>!Array.isArray(batch.requestedRevisions)||!batch.requestedRevisions.length||batch.requestedRevisions.length>policy.sourceCapture.revisionBatchSize||!Array.isArray(batch.response?.query?.pages)?[`batch_${index+1}_invalid`]:[]);return {distinctPinnedRevisionCount:expected.length,submittedRevisionCount:submitted.length,revisionBatchCount:batches.length,duplicateSubmittedRevisions,missingSubmittedRevisions,unexpectedSubmittedRevisions,invalidBatches,requestCoverageComplete:expected.length>0&&!duplicateSubmittedRevisions.length&&!missingSubmittedRevisions.length&&!unexpectedSubmittedRevisions.length&&!invalidBatches.length};}

export function auditStructuralMemberCandidateSemanticGapEvidence(records=[],{routingRecords=[],revisionBatches=[],policy={},contentHash=hash}={}){const compiled=compileStructuralMemberCandidateSemanticGapEvidencePolicy(policy),inputs=selectStructuralMemberCandidateSemanticGapEvidenceInputs(routingRecords,policy),expected=compiled.valid?inputs.map(input=>expectedRecord(input,revisionBatches,policy,contentHash)):[],inputKeys=inputs.map(item=>item.memberCandidateKey),outputKeys=records.map(item=>item.memberCandidateKey),duplicateInputKeys=duplicates(inputKeys),duplicateOutputKeys=duplicates(outputKeys),missingOutputKeys=inputKeys.filter(key=>!outputKeys.includes(key)),unexpectedOutputKeys=outputKeys.filter(key=>!inputKeys.includes(key)),recordMismatches=records.filter(record=>{const match=expected.find(item=>item.memberCandidateKey===record.memberCandidateKey);return !match||contentHash(record)!==contentHash(match)}).map(item=>item.memberCandidateKey),upstreamMismatches=records.filter(record=>{const input=inputs.find(item=>item.memberCandidateKey===record.memberCandidateKey);return !input||contentHash(preservedInput(record))!==contentHash(preservedInput(input))}).map(item=>item.memberCandidateKey),requestCoverage=revisionRequestCoverage(inputs,revisionBatches,policy),sources=records.flatMap(record=>record.structuralMemberCandidateSemanticGapEvidenceSources||[]),packets=records.flatMap(record=>record.structuralMemberCandidateSemanticGapEvidencePackets||[]),expectedWorkItemKeys=inputs.flatMap(input=>allWorkItems(input.structuralMemberCandidateSemanticGapEvidenceWorkRouting).map(item=>item.workItemKey)),packetKeys=packets.map(packet=>packet.workItemKey),duplicatePacketKeys=duplicates(packetKeys),missingPacketKeys=expectedWorkItemKeys.filter(key=>!packetKeys.includes(key)),unexpectedPacketKeys=packetKeys.filter(key=>!expectedWorkItemKeys.includes(key)),channels=packets.flatMap(packet=>packet.channelObservations||[]),channelCountMismatches=packets.filter(packet=>packet.requiredChannels.length!==packet.channelObservations.length||packet.requiredChannels.some(channel=>packet.channelObservations.filter(item=>item.channel===channel).length!==1)).map(packet=>packet.workItemKey),unsupportedPromotions=records.filter(record=>{const summary=record.structuralMemberCandidateSemanticGapEvidence||{},review=record.structuralMemberCandidateSemanticGapEvidenceReview||{},route=record.structuralMemberCandidateSemanticGapEvidenceWorkRouting||{};return summary.semanticEvidenceWorkComplete!==false||summary.memberUniverseComplete!==false||summary.automaticVerificationApplied!==false||review.semanticEvidenceWorkComplete!==false||review.memberUniverseComplete!==false||review.automaticVerificationApplied!==false||packets.some(packet=>packet.evidenceWorkComplete!==false||packet.memberUniverseComplete!==false||packet.automaticVerificationApplied!==false||['candidateMemberIdentityVerdict','sourcePageEntityTypeVerdict','structuralParentRelationshipVerdict','weightedTaskEntryMembershipVerdict','mappingVerdict','inventoryCompletenessVerdict'].some(field=>packet[field]!==null))||route.evidenceKeyCount!==0||route.completedWorkItemCount!==0||record.memberExpansionReview?.state!=='unreviewed'||record.mechanicsReview?.state!=='unreviewed'||record.optimizerEligible!==false;}).map(item=>item.memberCandidateKey),accountFindings=accountStateFindings(records),blockers=[];if(!compiled.valid)blockers.push('semantic_gap_evidence_policy_invalid_or_page_specific');if(routingRecords.length!==inputs.length||duplicateInputKeys.length)blockers.push('input_semantic_gap_route_set_not_exactly_eligible_and_unique');if(duplicateOutputKeys.length||missingOutputKeys.length||unexpectedOutputKeys.length)blockers.push('input_output_semantic_gap_evidence_record_set_mismatch');if(!requestCoverage.requestCoverageComplete)blockers.push('exact_revision_request_set_incomplete_or_mismatched');if(sources.some(source=>!source.captureComplete))blockers.push('one_or_more_exact_revision_sources_failed_revalidation');if(duplicatePacketKeys.length||missingPacketKeys.length||unexpectedPacketKeys.length)blockers.push('semantic_gap_evidence_packet_set_incomplete_or_mismatched');if(channelCountMismatches.length)blockers.push('required_channel_observation_set_incomplete_or_duplicated');if(recordMismatches.length)blockers.push('one_or_more_semantic_gap_evidence_records_do_not_match_exact_sources');if(upstreamMismatches.length)blockers.push('input_evidence_dispositions_routing_or_prior_sources_changed');if(unsupportedPromotions.length)blockers.push('semantic_gap_evidence_capture_created_unsupported_verdict_or_promotion');if(accountFindings.length)blockers.push('current_account_state_present');const publishable=compiled.valid&&routingRecords.length===inputs.length&&!duplicateInputKeys.length&&!duplicateOutputKeys.length&&!missingOutputKeys.length&&!unexpectedOutputKeys.length&&requestCoverage.requestCoverageComplete&&sources.length>0&&sources.every(source=>source.captureComplete)&&!duplicatePacketKeys.length&&!missingPacketKeys.length&&!unexpectedPacketKeys.length&&!channelCountMismatches.length&&!recordMismatches.length&&!upstreamMismatches.length&&!unsupportedPromotions.length&&!accountFindings.length,evidenceCaptureCoverageComplete=publishable&&packets.length===expectedWorkItemKeys.length&&packets.every(packet=>packet.capturePacketComplete);return {contract:policy.auditContract,inputCoverage:{inputRoutingRecordCount:routingRecords.length,eligibleRoutingRecordCount:inputs.length,evidenceRecordCount:records.length,duplicateInputKeys,duplicateOutputKeys,missingOutputKeys,unexpectedOutputKeys},policyCoverage:compiled,revisionRequestCoverage:requestCoverage,sourceCoverage:{distinctRevisionPinnedSourceCount:sources.length,completeExactRevisionSourceCount:sources.filter(source=>source.captureComplete).length,incompleteExactRevisionSourceCount:sources.filter(source=>!source.captureComplete).length,duplicateSourceKeys:duplicates(sources.map(source=>source.sourceKey))},workItemPacketCoverage:{routedWorkItemCount:expectedWorkItemKeys.length,evidencePacketCount:packets.length,completeCapturePacketCount:packets.filter(packet=>packet.capturePacketComplete).length,duplicatePacketKeys,missingPacketKeys,unexpectedPacketKeys},channelObservationCoverage:{requiredChannelObservationCount:packets.reduce((sum,packet)=>sum+packet.requiredChannels.length,0),capturedChannelObservationCount:channels.length,missingAuthoritativeEvidenceObservationCount:channels.filter(item=>item.captureState==='authoritative_evidence_not_observed_in_defined_source_set').length,pendingReviewObservationCount:channels.filter(item=>item.captureState==='review_decision_pending').length,insufficientOrUnresolvedObservationCount:channels.filter(item=>item.captureState.includes('insufficient')||item.captureState.includes('unresolved')).length,channelCountMismatches},semanticPreservationCoverage:{recordMismatches,upstreamMismatches,unsupportedPromotions},accountStateFindings:accountFindings,evidenceCaptureCoverageComplete,semanticEvidenceWorkComplete:false,memberIdentityReviewComplete:false,memberExpansionComplete:false,mechanicsReviewComplete:false,optimizerEligibleCount:0,completeActivityUniverse:false,absoluteBestGate:'blocked_incomplete_activity_universe',blockers:unique([...blockers,evidenceCaptureCoverageComplete?'revision_pinned_structural_member_candidate_semantic_gap_evidence_captured_reviews_pending':'one_or_more_revision_pinned_semantic_gap_evidence_packets_incomplete','candidate_member_identity_and_variant_scope_review_pending',...(packets.some(packet=>packet.branchKey==='unresolved_source_page_entity_type_evidence')?['one_or_more_candidate_source_pages_remain_untyped']:[]),'weighted_parent_task_entry_membership_not_proven','source_inventory_non_exhaustive_qualification_prevents_completeness','one_to_one_mapping_between_structural_candidates_and_declared_total_not_proven','member_universe_completeness_not_proven','all_repeatability_evidence_domains_remain_unresolved','requirements_xp_timing_and_mechanics_not_structured','independent_complete_activity_universe_not_established']),publishable};}

export function buildStructuralMemberCandidateSemanticGapEvidence({routingRecords=[],revisionBatches=[],policy={},contentHash=hash}){const compiled=compileStructuralMemberCandidateSemanticGapEvidencePolicy(policy),records=compiled.valid?selectStructuralMemberCandidateSemanticGapEvidenceInputs(routingRecords,policy).map(input=>expectedRecord(input,revisionBatches,policy,contentHash)):[];return {records,audit:auditStructuralMemberCandidateSemanticGapEvidence(records,{routingRecords,revisionBatches,policy,contentHash})};}
