import path from 'node:path';import {audit,writeSnapshot} from './lib.mjs';import {wikiRevisions,WIKI_API} from './activity-evidence-lib.mjs';import {parseAgilityRockslideGuideMembers} from './agility-rockslide-guide-member-lib.mjs';
const root=path.resolve(process.argv.find(argument=>argument.startsWith('--out='))?.slice(6)||'.platform-data'),title='Agility training',pages=await wikiRevisions([title]),page=pages.find(candidate=>candidate.title===title),revision=page?.revisions?.[0],parsed=parseAgilityRockslideGuideMembers({title:page?.title,content:revision?.slots?.main?.content||'',sourceRevision:revision?.revid,sourceTimestamp:revision?.timestamp,sourceUrl:'https://oldschool.runescape.wiki/w/Agility_training'}),structural=audit(parsed.records,{minimum:4,required:['memberKey','candidateKey','name','sourceOrder','pairingPolicy','sourceOpenEndedPairingUniverse','sourceRevision','sourceTimestamp','sourceUrl','sourceLocator'],maximumUnknownRatio:0}),sourceAudit={...parsed.audit,findings:structural.findings,publishable:parsed.audit.publishable&&structural.publishable};
const snapshot=await writeSnapshot(root,'agility-rockslide-guide-members',parsed.records,{kind:'osrs_wiki_agility_training_guide_rockslide_named_pairing_inventory',api:WIKI_API,pages:[title],coverageScope:'core_detour_and_every_named_pairing_in_the_levels_78_plus_rockslide_section',audit:sourceAudit});console.log(JSON.stringify({manifest:snapshot.manifest,members:parsed.records.map(record=>({memberKey:record.memberKey,candidateKey:record.candidateKey,name:record.name})),coverageLimitations:parsed.audit.coverageLimitations,findings:[...parsed.audit.blockers,...structural.findings]},null,2));
