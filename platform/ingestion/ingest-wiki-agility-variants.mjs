import path from 'node:path';import {audit,hash,writeSnapshot} from './lib.mjs';import {enrichAgilityVariantsWithSupportingEvidence,parseAgilityVariants,parseShayzienBasicSupportingEvidence} from './agility-variant-lib.mjs';import {WIKI_API,wikiCategoryMembers,wikiRevisions} from './activity-evidence-lib.mjs';
const root=path.resolve(process.argv.find(x=>x.startsWith('--out='))?.slice(6)||'.platform-data'),members=await wikiCategoryMembers('Agility courses'),pages=await wikiRevisions(members.map(x=>x.title)),rawRecords=[];
for(const page of pages){const revision=page.revisions?.[0],content=revision?.slots?.main?.content||'',sourceUrl=`https://oldschool.runescape.wiki/w/${encodeURIComponent(page.title.replace(/ /g,'_'))}`;for(const record of parseAgilityVariants({title:page.title,content,sourceRevision:revision?.revid,sourceTimestamp:revision?.timestamp,sourceUrl}))rawRecords.push(record)}
const supportingPages=await wikiRevisions(['Agility']),supportingEvidence=supportingPages.flatMap(page=>{const revision=page.revisions?.[0];return revision?parseShayzienBasicSupportingEvidence({title:page.title,content:revision.slots?.main?.content||'',sourceRevision:revision.revid,sourceTimestamp:revision.timestamp,sourceUrl:`https://oldschool.runescape.wiki/w/${encodeURIComponent(page.title.replace(/ /g,'_'))}`}):[]}),records=enrichAgilityVariantsWithSupportingEvidence(rawRecords,supportingEvidence).map(record=>({...record,content_hash:hash(record)}));
const report=audit(records,{minimum:4,required:['parent_name','variant_key','name','entry_level','xp_per_lap','cycle_seconds','source_revision','source_locator'],maximumUnknownRatio:0});if(!supportingEvidence.some(row=>row.parent_name==='Shayzien Agility Course'&&row.variant_key==='basic'))report.findings.push({severity:'blocker',rule:'shayzien_basic_supporting_evidence_missing'});for(const row of records)for(const conflict of row.supporting_evidence_conflicts||[])report.findings.push({severity:'record_blocker',record:row.record_key,...conflict});report.publishable=!report.findings.some(finding=>finding.severity==='blocker');
const snapshot=await writeSnapshot(root,'agility-variants',records,{kind:'osrs_wiki_composite_activity_variants_with_revision_pinned_supporting_evidence',api:WIKI_API,category:'Agility courses',categoryMembers:members.length,supportingEvidence:supportingEvidence.map(row=>({evidenceKey:row.evidence_key,sourceRevision:row.source_revision,sourceUrl:row.source_url})),audit:report});console.log(JSON.stringify({manifest:snapshot.manifest,publishable:report.publishable,records:records.length,supportingEvidence:supportingEvidence.length,variants:records.map(x=>({name:x.name,entryLevel:x.entry_level,xpPerLap:x.xp_per_lap,cycleSeconds:x.cycle_seconds,sourceWarning:x.source_warning})),findings:report.findings},null,2));
