import path from 'node:path';import {audit,fetchJson,hash,writeSnapshot} from './lib.mjs';import {parseAgilityVariants} from './agility-variant-lib.mjs';import {WIKI_API,wikiCategoryMembers,wikiRevisions} from './activity-evidence-lib.mjs';
const root=path.resolve(process.argv.find(x=>x.startsWith('--out='))?.slice(6)||'.platform-data'),members=await wikiCategoryMembers('Agility courses'),pages=await wikiRevisions(members.map(x=>x.title)),records=[];
for(const page of pages){const revision=page.revisions?.[0],content=revision?.slots?.main?.content||'',sourceUrl=`https://oldschool.runescape.wiki/w/${encodeURIComponent(page.title.replace(/ /g,'_'))}`;for(const record of parseAgilityVariants({title:page.title,content,sourceRevision:revision?.revid,sourceTimestamp:revision?.timestamp,sourceUrl}))records.push({...record,content_hash:hash(record)})}
const report=audit(records,{minimum:4,required:['parent_name','variant_key','name','entry_level','xp_per_lap','cycle_seconds','source_revision','source_locator'],maximumUnknownRatio:0}),snapshot=await writeSnapshot(root,'agility-variants',records,{kind:'osrs_wiki_composite_activity_variants',api:WIKI_API,category:'Agility courses',categoryMembers:members.length,audit:report});console.log(JSON.stringify({manifest:snapshot.manifest,publishable:report.publishable,records:records.length,variants:records.map(x=>({name:x.name,entryLevel:x.entry_level,xpPerLap:x.xp_per_lap,cycleSeconds:x.cycle_seconds,sourceWarning:x.source_warning})),findings:report.findings},null,2));
