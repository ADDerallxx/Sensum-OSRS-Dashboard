const vectors=[{name:'Complete proposed',conditions:{entryLevel:1,modeledMinimumLevel:1},state:'proposed',calculation:{xpPerHour:10000},approved:false},{name:'Unknown candidate',conditions:{entryLevel:null,modeledMinimumLevel:null},state:'blocked',calculation:null,approved:false},{name:'Failure-prone at account level',conditions:{entryLevel:30,modeledMinimumLevel:75},state:'proposed',calculation:{xpPerHour:44000},approved:false}];
const eligible=vectors.filter(x=>Number(x.conditions.entryLevel||Infinity)<=34),applicable=eligible.filter(x=>Number(x.conditions.modeledMinimumLevel||Infinity)<=34),approved=applicable.filter(x=>x.approved),unknown=vectors.filter(x=>!x.conditions.entryLevel||(Number(x.conditions.entryLevel)<=34&&Number(x.conditions.modeledMinimumLevel||Infinity)>34)),provisional=applicable.filter(x=>x.state==='proposed').sort((a,b)=>b.calculation.xpPerHour-a.calculation.xpPerHour)[0];
const unknownReasons=[...(!vectors[1].conditions.entryLevel?['eligibility_unknown']:[]),...(vectors[1].conditions.entryLevel>34?['level_requirement_not_met']:[])],claim=approved.length?'best_approved':provisional?'provisional':'insufficient_data',failures=[];if(claim!=='provisional')failures.push('Unapproved complete evidence must be labeled provisional.');if(unknown.length!==2)failures.push('Unknown eligibility and missing current-level models must remain visible.');if(unknownReasons.includes('level_requirement_not_met'))failures.push('Unknown eligibility must not be mislabeled as a failed level requirement.');if(provisional?.name!=='Complete proposed')failures.push('A model that starts above the account level cannot become the provisional leader.');if(approved.length)failures.push('Unapproved vectors cannot become winners.');if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Recommendation certificate safety checks passed.');
