import {parseAgilityModifierVariants} from '../ingestion/agility-modifier-variant-lib.mjs';
const meta=title=>({title,sourceRevision:'1',sourceTimestamp:'2026-01-01',sourceUrl:'https://example.test'});
const poll=`Available to players with an [[Agility]] level of 70.\nCompleting the course rewards a total of 890 Agility experience, which is increased to 1016 experience after completing the [[Desert Hard Diary]].\nWith 101 ticks (60.6 seconds) per lap, it is possible to gain around 52,300 exp per hour without the diary, and 60,300 exp per hour with the diary.`;
const seers=`Available to players with an [[Agility]] level of 60.\nPlayers gain 570 experience in total per completed lap of the course.\nThese obstacles will stop failing at 79 Agility.\nA perfect lap takes 73 ticks, or 43.8 seconds (~37 seconds with Seers bank teleport), meaning that at maximum efficiency, the maximum amount of experience gained is 46,600 per hour, or 56,000 per hour with Seers bank teleport.\nCompleting the [[hard Kandarin Diary]] for the [[Camelot Teleport]] in front of Seers' bank will cut time and, in addition to increasing the experience gained per hour, result in more marks.\n|None\n|13.8\n|-\n|Easy\n|14.4\n|-\n|Medium\n|15.0\n|-\n|Hard\n|16.6`;
const ard=`Available to players with an [[Agility]] level of 90.\nA full lap of this course can be completed in 45.6 seconds (76 [[tick]]s) making the maximum experience per hour 70,184 assuming perfect laps.\nCompleting the [[elite Ardougne Diary]] will increase the chance of receiving marks of grace on this course by 25%. This translates to 22.6 marks per hour.\nThis results in a theoretical maximum of 20 tokens per hour without the diary, and 25 per hour with the diary.`;
const rell=`Available to players with an [[Agility]] level of 80.\nA full lap of this Agility course can be done in 51.0 seconds (85 [[tick]]s), allowing a maximum rate of 55,052.4 experience per hour assuming perfect laps. This rate is increased to a maximum rate of 64,941.2 experience per hour with the completion of the [[hard Fremennik Diary]]. The rate can be increased even further, albeit very slightly (roughly 600 xp/hr), through use of the teleport on [[Fremennik sea boots 4]].`;
const rows=[...parseAgilityModifierVariants({...meta('Pollnivneach Rooftop Course'),content:poll}),...parseAgilityModifierVariants({...meta("Seers' Village Rooftop Course"),content:seers}),...parseAgilityModifierVariants({...meta('Ardougne Rooftop Course'),content:ard}),...parseAgilityModifierVariants({...meta('Rellekka Rooftop Course'),content:rell})],failures=[],check=(ok,message)=>{if(!ok)failures.push(message)};
check(rows.length===12,'Four rooftop modifier families must emit twelve independent variants.');
check(rows.find(x=>x.variant_key==='desert_hard')?.xp_per_lap===1016,'Pollnivneach diary XP must remain exact.');
check(rows.find(x=>x.variant_key==='hard_bank_teleport')?.cycle_seconds===null&&rows.find(x=>x.variant_key==='hard_bank_teleport')?.observed_rate_approximate===true,'Approximate Seers teleport timing must not become exact mechanics.');
check(rows.find(x=>x.variant_key==='ardougne_elite')?.objective_metrics?.marks_of_grace_per_hour_average===22.6,'Ardougne average marks must remain separate from theoretical marks.');
check(rows.find(x=>x.variant_key==='sea_boots_4')?.cycle_seconds===null&&rows.find(x=>x.variant_key==='sea_boots_4')?.axis_coverage.includes('equipment_modifier'),'Approximate sea-boots improvement must remain blocked from exact cycle calculation.');
check(rows.every(x=>x.source_locator?.evidence?.length&&x.entry_level),'Every modifier variant must retain sourced eligibility.');
if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Agility modifier variant checks passed.');
