import {verifyAgilityVariantExpansion} from '../transforms/agility-variant-exit-lib.mjs';
const detail={state:'expanded',source_revision:'1',source_url:'https://example.test',source_locators:[{line:1,excerpt:'evidence'}]},base={contract:'sensum.activity-variant-audit.v1',completeCoverage:true,findings:2,expanded:2,pending:0,pendingPages:[],byAxis:{equipment_modifier:{findings:1,pending:0},temporary_boost:{findings:1,pending:0}},variantSnapshotRejections:[],details:[detail,detail],absoluteBestActivityGate:'eligible_for_next_gate',generatedAt:'now',contentHash:'hash'},failures=[],check=(ok,message)=>{if(!ok)failures.push(message)};
check(verifyAgilityVariantExpansion(base).passed===true,'A complete traceable audit must pass the phase exit gate.');
for(const [name,mutation] of [['pending',{pending:1,completeCoverage:false}],['rejection',{variantSnapshotRejections:[{domain:'x'}]}],['locator',{details:[{...detail,source_locators:[]},detail]}],['axis',{byAxis:{equipment_modifier:{findings:1,pending:1}}}]]){const result=verifyAgilityVariantExpansion({...base,...mutation});check(result.passed===false&&result.blockers.length>0,`${name} regression must block phase exit.`)}
if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('Agility variant phase-exit checks passed.');
